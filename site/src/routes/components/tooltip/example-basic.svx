---
slug: components/tooltip/example-basic
title: A basic Tooltip
---

# Basic Tooltip

A very simple way and common way to have a tooltip is to use CSS to show it above the element in question, with a little arrow pointing at the element itself.

<script>
    import { tooltip, Tooltip } from '../../../../../src/index.js'
</script>

<style>
    button {
        margin: 2rem 1rem;
        padding: 1rem;
    }
	.tooltip {
		background-color: black;
		border-radius: .25rem;
		color: white;
		position: fixed;
		padding: .25rem .5rem;
		transform: translateX(-50%);
	}
	.tooltip:before {
		border-style: solid;
		border-width: 5px 5px 0 5px;
		border-color: black transparent transparent transparent ;
		content: '';
		height: 0;
		position: absolute;
		left: calc(50% - 5px);
		bottom: -5px;
		width: 0;
	}
</style>

<button use:tooltip="{{text: 'Squirrel'}}">🐿️</button>
<button use:tooltip="{{text: 'Rabbit'}}">🐇</button>
<button use:tooltip="{{text: 'Hamster'}}">🐹</button>

<Tooltip let:options let:position>
	{#if options}
		<div class="tooltip" style="left: {position.x + (position.width / 2)}px; top: calc({position.y}px - 2.75rem - 5px);">
			<span>{options.text}</span>
		</div>
	{/if}
</Tooltip>

```svelte
<script>
    import { tooltip, Tooltip } from 'renderless-svelte'
</script>

<style>
    button {
        margin: 2rem 1rem;
        padding: 1rem;
    }
	.tooltip {
		background-color: black;
		border-radius: .25rem;
		color: white;
		position: fixed;
		padding: .25rem .5rem;
		transform: translateX(-50%);
	}
	.tooltip:before {
		border-style: solid;
		border-width: 5px 5px 0 5px;
		border-color: black transparent transparent transparent ;
		content: '';
		height: 0;
		position: absolute;
		left: calc(50% - 5px);
		bottom: -5px;
		width: 0;
	}
</style>

<button use:tooltip="{{text: 'Squirrel'}}">🐿️</button>
<button use:tooltip="{{text: 'Rabbit'}}">🐇</button>
<button use:tooltip="{{text: 'Hamster'}}">🐹</button>

<Tooltip let:options let:position>
	{#if options}
		<div class="tooltip" style="left: {position.x + (position.width / 2)}px; top: calc({position.y}px - 2.75rem - 5px);">
			<span>{options.text}</span>
		</div>
	{/if}
</Tooltip>
```